#pragma once
#include "student.h"
namespace GlobalFunctions {
	int byName(student a, student b);
	int byMath(student a, student b);
	int byEng(student a, student  b);
	int byHist(student a, student b);
	int byAverage(student a, student b);
}